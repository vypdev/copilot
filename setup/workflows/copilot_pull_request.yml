name: Copilot - Pull Request

on:
  pull_request:
    types: [opened, reopened, edited, closed, synchronize]
  pull_request_review:
    types: [submitted, edited, dismissed]
  merge_group:
    types: [checks_requested]

jobs:
  copilot-merge-group:
    if: ${{ github.event_name == 'merge_group' }}
    name: Copilot - Pull Request
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      checks: write
      contents: read
    steps:
      - name: Confirm merge-group compatibility
        run: echo "Copilot PR analysis already ran on each constituent pull request."

  copilot-pull-requests:
    if: ${{ (vars.COPILOT_BOT_LOGIN == '' || github.actor != vars.COPILOT_BOT_LOGIN) && github.event.pull_request.head.repo.full_name == github.repository }}
    name: Copilot - Pull Request
    runs-on: ubuntu-latest
    timeout-minutes: 120
    concurrency:
      group: copilot-bugbot-${{ github.repository }}-${{ github.event.pull_request.head.ref || github.ref_name }}
      cancel-in-progress: true
    permissions:
      checks: write
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Set up Node.js 24 for pinned agent installation
        uses: actions/setup-node@v7
        with:
          node-version: '24.x'

      - name: Fetch incremental review range
        if: github.event.action == 'synchronize'
        env:
          BEFORE_SHA: ${{ github.event.before }}
          AFTER_SHA: ${{ github.event.after }}
        run: |
          git cat-file -e "${AFTER_SHA}^{commit}"
          if ! git cat-file -e "${BEFORE_SHA}^{commit}"; then
            echo "::notice::The previous PR commit is unavailable; Bugbot will use the canonical pull-request diff."
          fi

      - uses: vypdev/copilot@v3
        with:
          ai-ignore-files: ${{ vars.AI_IGNORE_FILES || 'build/*' }}
          debug: ${{ vars.DEBUG }}
          main-branch: ${{ vars.MAIN_BRANCH || 'master' }}
          development-branch: ${{ vars.DEVELOPMENT_BRANCH || 'develop' }}
          feature-tree: ${{ vars.FEATURE_TREE || 'feature' }}
          bugfix-tree: ${{ vars.BUGFIX_TREE || 'bugfix' }}
          hotfix-tree: ${{ vars.HOTFIX_TREE || 'hotfix' }}
          release-tree: ${{ vars.RELEASE_TREE || 'release' }}
          docs-tree: ${{ vars.DOCS_TREE || 'docs' }}
          chore-tree: ${{ vars.CHORE_TREE || 'chore' }}
          branch-management-always: ${{ vars.BRANCH_MANAGEMENT_ALWAYS || 'false' }}
          reopen-issue-on-push: ${{ vars.REOPEN_ISSUE_ON_PUSH || 'true' }}
          desired-assignees-count: ${{ vars.DESIRED_ASSIGNEES_COUNT || '1' }}
          desired-reviewers-count: ${{ vars.DESIRED_REVIEWERS_COUNT || '1' }}
          issues-locale: ${{ vars.ISSUES_LOCALE || 'en-US' }}
          pull-requests-locale: ${{ vars.PULL_REQUESTS_LOCALE || 'en-US' }}
          commit-prefix-transforms: ${{ vars.COMMIT_PREFIX_TRANSFORMS || 'replace-slash' }}
          ai-pull-request-description-mode: ${{ vars.AI_PULL_REQUEST_DESCRIPTION_MODE || 'replace' }}
          ai-members-only: ${{ vars.AI_MEMBERS_ONLY || 'false' }}
          ai-include-reasoning: ${{ vars.AI_INCLUDE_REASONING || 'false' }}
          bugbot-severity: ${{ vars.BUGBOT_SEVERITY || 'low' }}
          bugbot-comment-limit: ${{ vars.BUGBOT_COMMENT_LIMIT || '20' }}
          bugbot-fix-verify-commands: ${{ vars.BUGBOT_AUTOFIX_VERIFY_COMMANDS }}
          bugbot-dry-run: ${{ vars.BUGBOT_DRY_RUN || 'false' }}
          bugbot-effort: ${{ vars.BUGBOT_EFFORT || 'smart' }}
          bugbot-review-drafts: ${{ vars.BUGBOT_REVIEW_DRAFTS || 'false' }}
          bugbot-trace-rules: ${{ vars.BUGBOT_TRACE_RULES || 'false' }}
          bugbot-suggested-changes: ${{ vars.BUGBOT_SUGGESTED_CHANGES || 'true' }}
          bugbot-telemetry: ${{ vars.BUGBOT_TELEMETRY || 'true' }}
          bugbot-fail-on-unresolved: ${{ vars.BUGBOT_FAIL_ON_UNRESOLVED || 'false' }}
          bugbot-organization-rules: ${{ vars.BUGBOT_ORGANIZATION_RULES }}
          project-column-issue-created: ${{ vars.PROJECT_COLUMN_ISSUE_CREATED || 'Todo' }}
          project-column-pull-request-created: ${{ vars.PROJECT_COLUMN_PULL_REQUEST_CREATED || 'In Progress' }}
          project-column-issue-in-progress: ${{ vars.PROJECT_COLUMN_ISSUE_IN_PROGRESS || 'In Progress' }}
          project-column-pull-request-in-progress: ${{ vars.PROJECT_COLUMN_PULL_REQUEST_IN_PROGRESS || 'In Progress' }}
          agent-provider: ${{ vars.AGENT_PROVIDER || 'codex' }}
          agent-model-provider: ${{ vars.AGENT_MODEL_PROVIDER || 'openai' }}
          agent-model: ${{ vars.AGENT_MODEL || 'gpt-5.6-luna' }}
          agent-effort: ${{ vars.AGENT_EFFORT }}
          agent-executable: ${{ vars.AGENT_EXECUTABLE }}
          findings-provider: ${{ vars.FINDINGS_PROVIDER }}
          findings-model-provider: ${{ vars.FINDINGS_MODEL_PROVIDER }}
          findings-model: ${{ vars.FINDINGS_MODEL }}
          findings-effort: ${{ vars.FINDINGS_EFFORT }}
          findings-executable: ${{ vars.FINDINGS_EXECUTABLE }}
          fixer-provider: ${{ vars.FIXER_PROVIDER }}
          fixer-model-provider: ${{ vars.FIXER_MODEL_PROVIDER }}
          fixer-model: ${{ vars.FIXER_MODEL }}
          fixer-effort: ${{ vars.FIXER_EFFORT }}
          fixer-executable: ${{ vars.FIXER_EXECUTABLE }}
          planner-provider: ${{ vars.PLANNER_PROVIDER }}
          planner-model-provider: ${{ vars.PLANNER_MODEL_PROVIDER }}
          planner-model: ${{ vars.PLANNER_MODEL }}
          planner-effort: ${{ vars.PLANNER_EFFORT }}
          planner-executable: ${{ vars.PLANNER_EXECUTABLE }}
          reviewer-provider: ${{ vars.REVIEWER_PROVIDER }}
          reviewer-model-provider: ${{ vars.REVIEWER_MODEL_PROVIDER }}
          reviewer-model: ${{ vars.REVIEWER_MODEL }}
          reviewer-effort: ${{ vars.REVIEWER_EFFORT }}
          reviewer-executable: ${{ vars.REVIEWER_EXECUTABLE }}
          project-ids: ${{ vars.PROJECT_IDS }}
          token: ${{ secrets.PAT }}
        env:
          AGENT_PROVIDER: ${{ vars.AGENT_PROVIDER || 'codex' }}
          AGENT_MODEL_PROVIDER: ${{ vars.AGENT_MODEL_PROVIDER || 'openai' }}
          AGENT_MODEL: ${{ vars.AGENT_MODEL }}
          AGENT_EFFORT: ${{ vars.AGENT_EFFORT }}
          AGENT_PROVISIONING: ${{ vars.AGENT_PROVISIONING || 'auto' }}
          AGENT_ALLOWED_MODEL_PROVIDERS: ${{ vars.AGENT_ALLOWED_MODEL_PROVIDERS || 'openai' }}
          AGENT_ALLOWED_MODELS: ${{ vars.AGENT_ALLOWED_MODELS || 'openai/gpt-5.6-luna' }}
          AGENT_EXECUTABLE: ${{ vars.AGENT_EXECUTABLE }}
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          CURSOR_API_KEY: ${{ (vars.AGENT_PROVIDER == 'cursor' || vars.FINDINGS_PROVIDER == 'cursor' || vars.FIXER_PROVIDER == 'cursor' || vars.PLANNER_PROVIDER == 'cursor' || vars.REVIEWER_PROVIDER == 'cursor' || vars.TESTER_PROVIDER == 'cursor') && secrets.CURSOR_API_KEY || '' }}
          CODEX_API_KEY: ${{ secrets.CODEX_API_KEY }}
          COPILOT_EVIDENCE_TOKEN: ${{ github.token }}
