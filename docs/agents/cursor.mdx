---
title: Cursor runtime
description: Configure and validate Cursor as a hardened agent runtime.
---

# Cursor runtime

Copilot supports Cursor at the command, provisioning, authentication, sandbox, timeout, cancellation, output-limit, and terminal-failure boundaries. Production readiness is deployment-specific: run a real authenticated smoke test for the selected account, model, and runner image before enabling it in a protected workflow.

## Runtime contract

Copilot invokes Cursor non-interactively:

```text
agent -p --output-format text --sandbox enabled --model <model> [--mode ask|--force] <prompt>
```

Cursor must be preinstalled on the controlled runner and report the exact
manifest version:

```text
2026.09.10-fd3934a
CURSOR_API_KEY
```

Automatic installation cannot guarantee this immutable historical build and is
therefore unsupported. Unofficial npm packages, shell fallbacks, and unverified
downloads are not supported.

## Configuration

```yaml
with:
  agent-provider: cursor
  agent-model-provider: cursor
  agent-model: <approved-model>
  agent-effort: <optional-preference>
env:
  CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
```

The runtime provider (`cursor`) is separate from the model provider and model. Cursor's common command surface exposes model selection but no portable effort flag; Copilot keeps the common effort value in execution context instead of inventing an unsupported CLI option. Do not assume that a CLI installation proves that a selected model is available or authorized.

## Controlled execution environment

Copilot creates a private temporary home, an isolated `cli-config.json`, and the
documented `~/.cursor/sandbox.json` for every invocation. Analysis roles use Ask
mode plus an explicit `Write(**)` denial. The fixer role may use non-interactive
write mode only after authorization and workspace preflight. Both modes deny the
documented `Shell(*)`, `WebFetch(*)`, and `Mcp(*:*)` permission classes; the
sandbox denies network by default and adds no external paths. Any repository
authority-bearing project files (`cli.json`, `sandbox.json`, `mcp.json`, and
`hooks.json`) are rejected rather than merged. Repository rules may still guide
the task, but cannot widen the managed permission or sandbox boundary.

Only the Cursor credential and a small allowlist of process variables reach the child process. GitHub tokens, unrelated provider credentials, cloud secrets, and arbitrary job environment values are removed.

## Operator validation

Safe binary checks do not consume model usage:

```bash
agent --version
agent --help
node scripts/verify-agent-clis.cjs
```

An authenticated smoke test is separate. Run it only with explicit authorization and a bounded cost, using a read-only prompt that neither inspects nor modifies sensitive files. Confirm that the installer, model entitlement, billing policy, and sandbox behavior remain compatible with the target runner image.

## Security boundary

Never put `CURSOR_API_KEY` in action inputs, command arguments, prompts, repository files, or logs. Map it only to the Copilot action step in a protected workflow. Do not expose it to fork-controlled code.

## Related pages

- [Runtime selection](/agents/runtime-selection)
- [Agent CLI configuration](/agents/cli-configuration)
- [Agent CLI provisioning](/security-operations/operations/cli-provisioning)
- [Agent CLI runners](/security-operations/operations/cli-runners)
- [Official Cursor CLI configuration](https://cursor.com/docs/cli/reference/configuration)
- [Official Cursor CLI permissions](https://cursor.com/docs/cli/reference/permissions)
- [Official Cursor sandbox reference](https://cursor.com/docs/reference/sandbox)
