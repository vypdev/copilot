---
title: Bugbot
description: Detect potential problems, publish findings, and optionally apply trusted fixes.
---

# Bugbot

Bugbot analyzes changes with the configured agent CLI and publishes structured findings to issues and pull requests. It can optionally apply fixes or user-requested changes when the workflow is restricted to trusted actors and the verification commands pass.

<Warning>
The repository's currently validated Bugbot path uses OpenCode. Codex is an implemented CLI runtime with a narrower validation boundary, and Cursor remains WIP. See [runtime coverage](/features#ai-features-and-runtime-coverage) before selecting a non-OpenCode runtime.
</Warning>

## Requirements

- a configured `agent-provider`, `agent-model-provider`, and `agent-model`;
- the corresponding CLI and credential reference;
- GitHub permissions for the requested comment or write operation;
- protected workflows that do not expose credentials to untrusted code.

See [Agent CLI configuration](/agents/cli-configuration) and [Configuration](/bugbot/configuration).

## Quick summary

| What you do | What happens |
| --- | --- |
| **Push** to a branch linked to an issue | Bugbot analyzes the diff and posts findings on the issue and any open PR. |
| **Comment** “fix it” / “fix all” (with permission) | The selected agent applies the requested fixes; verification commands run before any commit or push. |
| **Comment** “add a test for X” (with permission) | The selected agent applies the request; verification commands run before any commit or push. |

## Pages

- [Detection](/bugbot/detection)
- [Autofix](/bugbot/autofix)
- [Do user request](/bugbot/do-user-request)
- [Configuration](/bugbot/configuration)
- [How it works](/bugbot/how-it-works)
- [Examples](/bugbot/examples)
