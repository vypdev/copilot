import type { WorkflowPollingRandomPort } from '../../application/ports/workflow_run_ports';
export declare class SystemWorkflowPollingRandomAdapter implements WorkflowPollingRandomPort {
    next(): number;
}
